% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{start_engine}
\alias{start_engine}
\title{Start a reusable MATLAB/FSDA engine session}
\usage{
start_engine(
  routine = NULL,
  python = Sys.getenv("FSDA_DEV_VENV"),
  fsda_root = NULL
)
}
\arguments{
\item{routine}{Optional name of an FSDA routine whose availability is
verified at startup (e.g. \code{"FSR"}). \code{NULL} skips the check.}

\item{python}{Path to the Python interpreter (or virtualenv root) that has
\code{matlabengine} installed. Defaults to the \code{FSDA_DEV_VENV} environment
variable, then to \code{python}/\code{python3} on the \code{PATH}.}

\item{fsda_root}{Path to the FSDA toolbox folder to add to the MATLAB path.
\code{NULL} assumes FSDA is already on the MATLAB path (e.g. an installed
Add-On).}
}
\value{
An object of class \code{"fsda_engine"} to pass to \code{\link[=fsda_call]{fsda_call()}},
\code{\link[=eval_m]{eval_m()}}, \code{\link[=diagnostics]{diagnostics()}} and \code{\link[=stop_engine]{stop_engine()}}.
}
\description{
Imports the bundled Python engine (\code{inst/python/engine.py}) through
reticulate and starts a MATLAB engine session with the FSDA toolbox on its
path. Starting MATLAB is expensive (tens of seconds); keep the returned
handle and reuse it for many \code{\link[=fsda_call]{fsda_call()}} invocations, then release it
with \code{\link[=stop_engine]{stop_engine()}}.
}
\examples{
\dontrun{
h = start_engine("FSR", fsda_root = "~/FSDA")
out = fsda_call(h, "FSR", y, X, nsamp = 0, intercept = TRUE, plots = 0)
stop_engine(h)
}
}
