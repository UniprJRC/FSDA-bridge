% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{fsda_call}
\alias{fsda_call}
\title{Call any FSDA routine through the engine}
\usage{
fsda_call(handle, name, ..., nargout = 1, echo_output = FALSE, options = NULL)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{name}{Name of the FSDA function to call (e.g. \code{"FSR"}, \code{"LXS"},
\code{"mahalFS"}).}

\item{...}{Positional arguments, then name/value options for the routine.}

\item{nargout}{Number of MATLAB outputs to request (default 1).}

\item{echo_output}{If \code{TRUE}, echo MATLAB console output.}

\item{options}{Optional named list merged into the name/value options.}
}
\value{
The routine's output converted to R (named list for structs/tables,
matrix/vector for numeric arrays); a list of outputs when \code{nargout > 1}.
}
\description{
Routine-agnostic call: positional \code{...} become MATLAB positional arguments
(in order), named \code{...} become MATLAB name/value option pairs. Pass \code{y} as
an \verb{(n, 1)} matrix when a routine expects a column vector. reticulate
converts R matrices to numpy arrays and the returned MATLAB struct/table to
a named list automatically.
}
\examples{
\dontrun{
out = fsda_call(h, "FSR", y, X, nsamp = 0, intercept = TRUE, plots = 0)
lxs = fsda_call(h, "LXS", y, X, intercept = TRUE, plots = 0, msg = 0)
}
}
