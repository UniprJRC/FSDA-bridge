% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FSM.R
\name{FSM}
\alias{FSM}
\title{Forward Search for Multivariate data (FSM)}
\usage{
FSM(handle, Y, ...)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{Y}{Data matrix (n, p) of observations.}

\item{...}{Name/value options passed straight through to FSM (e.g.
\code{plots}, \code{msg}, \code{init}).}
}
\value{
The FSM output converted to R (named list), including \code{mmd}
(the minimum Mahalanobis distance trajectory, as a two-column
step/value matrix).
}
\description{
Thin wrapper around \code{fsda_call()} for the FSDA \code{FSM} routine — robust
multivariate outlier detection via forward search, tracking the minimum
Mahalanobis distance (\code{mmd}) as units are added to the subset.
}
\examples{
\dontrun{
h = start_engine("FSM")
out = FSM(h, Y, plots = 0)
stop_engine(h)
}
}
