% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mahalFS.R
\name{mahalFS}
\alias{mahalFS}
\title{Mahalanobis Distance (mahalFS)
Thin wrapper around \code{fsda_call()} for the FSDA \code{mahalFS} routine —
computes the Mahalanobis distance of each observation in \code{Y} from a
given mean vector \code{MU} and covariance matrix \code{SIGMA}.}
\usage{
mahalFS(handle, Y, MU, SIGMA, ...)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{Y}{Data matrix (n, p) of observations.}

\item{MU}{Mean vector of length p.}

\item{SIGMA}{Covariance matrix (p, p).}

\item{...}{Additional name/value options passed straight through to
mahalFS.}
}
\value{
The Mahalanobis distances for each observation, as a numeric
vector.
}
\description{
Mahalanobis Distance (mahalFS)
Thin wrapper around \code{fsda_call()} for the FSDA \code{mahalFS} routine —
computes the Mahalanobis distance of each observation in \code{Y} from a
given mean vector \code{MU} and covariance matrix \code{SIGMA}.
}
\examples{
\dontrun{
h = start_engine("mahalFS")
d = mahalFS(h, Y, MU, SIGMA)
stop_engine(h)
}
}
